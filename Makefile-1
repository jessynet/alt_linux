CC=g++

CFLAGS=-c -Wall

all: libcompbranches.so

libcompbranches.so: compbranches.o
	$(CC) -shared -o libcompbranches.so compbranches.o

compbranches.o: compbranches.cpp
	$(CC) -fPIC $(CFLAGS) compbranches.cpp

clean:
	rm -rf *.o

